global:
  scrape_interval: 5s
  evaluation_interval: 5s

rule_files:
  - "prometheus_rules.yml"

scrape_configs:
  - job_name: "threat-api"
    metrics_path: /metrics
    static_configs:
      - targets: ["api:8000"]
        labels:
          env: "demo"
